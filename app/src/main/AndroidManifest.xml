<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.developer.vinay22ji.gamerscoin_arena">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GamersCoin_Arena">
        <activity android:name=".Activities.EditProfile_Activity"/>
        <activity android:name=".Activities.OnlineGames_Activity" />
        <activity android:name=".GamesActivity.PianoTiles_Activity" />
        <activity android:name=".Activities.Login_Activity" />
        <activity android:name=".Activities.WebActivity" />
        <activity android:name=".Activities.MainActivity" />
        <activity android:name=".Activities.Splace_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>